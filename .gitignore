__pycache__/
*.pyc
# Build output directory generated by scripts/build_site.py (pages are copied to repo root).
site/

# Weekly link-check workflow: keep local until the GitHub app has "workflows" permission,
# then git add -f and push to activate.
.github/workflows/link-check.yml
